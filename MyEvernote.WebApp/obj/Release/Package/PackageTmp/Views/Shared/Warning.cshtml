@using MyEvernote.WebApp.ViewModals
@model WarningViewModal

@{
    ViewBag.Title = Model.Title;
}

<div class="container">
    <div class="row">
        <div class="col-md-4 col-md-offset-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">@Model.Header</h3>
                </div>
                <div class="panel-body">
                    <div class="text-warning">
                        <h2><span class="glyphicon glyphicon-warning-sign"></span> @Model.Title</h2><hr />
                        <p class="text-justify">
                            @foreach (string msg in Model.Items)
                            {
                                @msg<br>
                            }
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@{
    string js = string.Empty;
    if (Model.IsRedirecting)
    {
        js = "setTimeout(function() { window.location = '" + Model.RedirectingUrl + "'; }, " + Model.RedirectingTimeOut + ");";
    }
}
<script>
    $(function () {
        @Html.Raw(js);
    });
</script>